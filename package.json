{
  "name": "lastfm-np-terminal",
  "version": "1.0.0",
  "description": "Simple terminal application that prints what a user is currently listening to through lastfm.",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Fredrik Reveny",
  "license": "ISC",
  "dependencies": {
    "requestify": "^0.2.0"
  }
}
